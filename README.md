# Hans Christian P. Santos — Portfolio

Personal portfolio: full-stack + embedded builds on a bench-log theme.
Static HTML/CSS/JS, no build step, no dependencies.


## Run locally

Open `index.html`, or:

python -m http.server
